package lruCache;

public class Test {
	public static void main(String[]  args) {
		LRUCache cache = new LRUCache(2);
		cache.set(2, 1);
		cache.print();
		cache.set(2, 2);	
		cache.print();
		cache.get(2);
		cache.print();
	}
}
