package maximumSubarray;

public class ON {
	public int maxSubArray(int[] A) {
		if (A == null || A.length == 0 ){
			return 0;
		}
		
		int sum = 0;
		if (A[0] > 0) {
			sum = A[0];
		}
		int max = 0;
		for (int i = 0; i < A.length; i++) {
			if (A[i] < 0 ) {
				if (i == 0) {
					
				}
			}
		}
	}
}
