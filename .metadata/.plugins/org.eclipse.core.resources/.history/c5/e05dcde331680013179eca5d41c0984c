package atoi;

public class AToI {
	public int atoi(String str) {
		if (str == null) {
			return 0;
		}
		str = str.trim();
		boolean positive = false;
		int pos = 0;
		int result = 0;
		int factor = 10;
		if (str.charAt(pos) == '+')
		return 0;
	}
}
