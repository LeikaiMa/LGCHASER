package sqrt;

public class Newton {
	public int sqrt(int x) {
		if (x == 0 || x == 1) {
			return x;
		}

		double last = 0;
		double res = 1;
		while (res != last) {
			last = res;
			res = (res + x / res) / 2;
		}

		return (int) res;
	}
}
