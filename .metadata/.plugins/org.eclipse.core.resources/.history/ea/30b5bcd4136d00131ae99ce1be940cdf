package jumpGame;

public class DP {
	public boolean canJump(int[] A) {
		if (A == null) {
			return false;
		}
		int len = A.length;
		int[] ways = new int[len];
		for (int i = 1; i < len - 1; i++) {
			
		}
	}
}
