package singleNumberII;

public class ThreeTmp {
	public int singleNumber(int[] A) {
		int one = 0;
		int two = 0;
		int three = 0;

		for (int i : A) {
			two |= one & i;
			one = one ^ i;
			three = ~(one & two);
			one = one & three;
			two = two & three;
		}

		return one;
	}
}
