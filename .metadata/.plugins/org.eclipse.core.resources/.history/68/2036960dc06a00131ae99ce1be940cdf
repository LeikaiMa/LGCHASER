package singleSumII;

public class BitManipulation {
	public static int singleNumber(int[] A) {
		int one = 0;
		int two = 0;
		int three = 0;
		
		for (int i = 0; i < A.length; i++) {
			two |= one & A[i];
			one = one ^ A[i];
			three = ~(one & two);
		}
	}
}
